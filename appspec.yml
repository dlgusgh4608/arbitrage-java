version: 0.0
os: linux

files:
  - source:  /
    destination: /home/ubuntu
    overwrite: yes

permissions:
  - object: /
    owner: ubuntu
    group: ubuntu

# https://docs.aws.amazon.com/ko_kr/codedeploy/latest/userguide/reference-appspec-file-structure-hooks.html  -> "인 플레이스(in-place) 배포"를 검색
hooks:
  ApplicationStop:
    - location: scripts/create-app-dir.sh
      timeout: 60
      runas: ubuntu
  BeforeInstall:
    - location: scripts/set-env.sh
      timeout: 60
      runas: ubuntu
  AfterInstall:
    - location: scripts/stop-server.sh
      timeout: 60
      runas: ubuntu
  ApplicationStart:
    - location: scripts/start-server.sh
      timeout: 60
      runas: ubuntu